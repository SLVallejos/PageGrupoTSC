# Nunca debe ser accesible por HTTP directo -- son scripts SQL, no algo
# que un visitante deba poder descargar.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
